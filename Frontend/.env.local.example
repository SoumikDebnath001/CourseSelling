# Base URL of the Cricket Academy courses API (Backend)
NEXT_PUBLIC_API_URL=http://localhost:4000/api/v1

# Optional: only if you serve media from a CUSTOM CDN domain (e.g. cdn.yoursite.com).
# Plain *.cloudfront.net domains work without this.
# NEXT_PUBLIC_CDN_HOSTNAME=cdn.yoursite.com
